import { CardParentHandlerDirective } from './card-parent-handler.directive';

describe('CardParentHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new CardParentHandlerDirective();
    expect(directive).toBeTruthy();
  });
});

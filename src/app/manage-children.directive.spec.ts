import { ManageChildrenDirective } from './manage-children.directive';

describe('ManageChildrenDirective', () => {
  it('should create an instance', () => {
    const directive = new ManageChildrenDirective();
    expect(directive).toBeTruthy();
  });
});
